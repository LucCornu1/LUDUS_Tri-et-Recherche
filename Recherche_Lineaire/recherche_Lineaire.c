#include <stdio.h>
#include <stdlib.h>
#include "recherche_Lineaire.h"

int searchInterpolationLinear(int taille,int tab[taille])
{
    int findvalue = 0;

    for(int i=0;i<=taille/2;i++)
    {

    }
}
